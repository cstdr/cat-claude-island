# 2026-04-13/14 临时分支 v1.3 合并进展

## 当前分支
`temp-merge-v1.3`

## 已完成的改动

### 1. Socket 权限修复 ✅
**文件:** `ClaudeIsland/Services/Hooks/HookSocketServer.swift`
- Line 177: `chmod(Self.socketPath, 0o777)` → `chmod(Self.socketPath, 0o600)`
- **安全修复**: 防止任意进程伪造 hook 事件

### 2. Task → Agent 工具名兼容 ✅
**文件:** `ClaudeIsland/Services/Chat/ChatHistoryManager.swift`
- 添加 `isSubagentContainer` 属性
- 添加 `isSubagentContainerName(_ name: String?)` 静态方法
- 添加 `case image(ImageBlock)` 到 `ChatHistoryItemType`
- 添加 `ImageBlock` 结构体

**文件:** `ClaudeIsland/Utilities/MCPToolFormatter.swift`
- 重排序类型检查（Bool 在 NSNumber 之前）

### 3. 新增 Hook 事件支持 ✅
**文件:** `ClaudeIsland/Services/Hooks/HookInstaller.swift`
- `PostToolUseFailure`
- `PermissionDenied`
- `StopFailure`
- `SubagentStart`
- `PostCompact`
- 使用 `ClaudePaths` 统一路径管理

### 4. ClaudePaths 统一路径管理 ✅
**新文件:** `ClaudeIsland/Core/ClaudePaths.swift`
- 支持 `CLAUDE_CONFIG_DIR` 环境变量
- 支持 `AppSettings.claudeDirectoryName` 自定义目录
- 线程安全缓存（NSLock）

**文件:** `ClaudeIsland/Core/Settings.swift`
- 添加 `claudeDirectoryName` 属性

### 5. ImageMessageView ✅
**文件:** `ClaudeIsland/UI/Views/ChatView.swift`
- 新增 `ImageMessageView` 结构体用于内联图片渲染
- 新增 `.image` case 到 `MessageItemView` switch

### 6. 本地化改动 ✅
**文件:** 多个 UI 文件
- NotchMenuView, CustomSoundRow, ScreenPickerRow, ClaudeInstancesView, ChatView, ToolResultViews
- 使用 `LanguageManager.shared.localized()` 和 `.localized` 扩展

**新文件:** `ClaudeIsland/Core/LanguageManager.swift`
- 运行时语言切换支持

### 7. 本地化文件 ✅
**文件:** `ClaudeIsland/en.lproj/Localizable.strings`, `ClaudeIsland/zh-Hans.lproj/Localizable.strings`
- 包含所有需要的 key

### 8. StableHash + ImageBlock 完善 ✅
**文件:** `ClaudeIsland/Models/ChatMessage.swift`
- 添加 `import CryptoKit`
- 添加 `StableHash` 枚举（稳定的 SHA256 哈希用于 ID）
- `ImageBlock` 添加 `id` 属性
- `MessageBlock` 的 `.image` case 添加 `id` 和 `typePrefix`

### 9. PermissionContext.formattedInput 改进 ✅
**文件:** `ClaudeIsland/Models/SessionPhase.swift`
- Bash: 优先显示 command
- Write/Edit/Read: 显示文件路径
- 默认: 使用优先 key 列表（command, file_path, path, query 等）
- 使用 `AnyCodable.value as?` 类型判断

### 10. ConversationParser 改进 ✅
**文件:** `ClaudeIsland/Services/Session/ConversationParser.swift`
- 添加 `UsageInfo` 结构体（token 追踪）
- 复用 `isoFormatter` 静态属性
- `sessionFilePath` 使用 `ClaudePaths.projectsDir.path`
- 添加 `subagentFilePath()` 函数
- 添加 `"Agent"` case 在 `formatToolInput` 和 `parseStructuredResult`
- 添加 `"image"` case 解析

### 11. 定期状态检查 (Dead-process detection) ✅
**文件:** `ClaudeIsland/Services/Session/ClaudeSessionMonitor.swift`
- `startPeriodicStatusCheck()` 和 `stopPeriodicStatusCheck()`

**文件:** `ClaudeIsland/Services/State/SessionStore.swift`
- 添加 `statusCheckTask` 和 `statusCheckIntervalSeconds`
- 添加 `isProcessRunning(pid:)` 函数
- `processSubagentTracking` 完整逻辑
- `syncSubagentToolsToChatItems` 函数

### 12. AgentFileWatcher 更新 ✅
**文件:** `ClaudeIsland/Services/Session/AgentFileWatcher.swift`
- 使用 `ConversationParser.subagentFilePath()`
- `parseTools()` 传递 `sessionId` 参数

### 13. JSONLInterruptWatcher 更新 ✅
**文件:** `ClaudeIsland/Services/Session/JSONLInterruptWatcher.swift`
- 使用 `ClaudePaths.projectsDir.path`

### 14. SessionState 更新 ✅
**文件:** `ClaudeIsland/Models/SessionState.swift`
- 添加 `usage` 属性返回 `UsageInfo`

### 15. AppDelegate 使用 ClaudePaths ✅
**文件:** `ClaudeIsland/App/AppDelegate.swift`
- 使用 `ClaudePaths.projectsDir` 替代硬编码路径

### 16. ScreenObserver 屏幕变化防抖 ✅
**文件:** `ClaudeIsland/App/ScreenObserver.swift`
- 添加 `pendingWork` 和 `debounceInterval: 0.5`
- `scheduleScreenChange()` 方法防止睡眠唤醒多次重建窗口

### 17. ToolResultData 修复 + Task/Agent ✅
**文件:** `ClaudeIsland/Models/ToolResultData.swift`
- `GenericResult ==` 比较 `rawData` 字典
- `"Agent"` case 添加到 `ToolStatusDisplay`

### 18. ToolEventProcessor 统一判断 ✅
**文件:** `ClaudeIsland/Services/State/ToolEventProcessor.swift`
- `event.tool == "Task"` → `ToolCallItem.isSubagentContainerName(event.tool)`

### 19. WindowManager 优化 ✅
**文件:** `ClaudeIsland/App/WindowManager.swift`
- `isInitialLaunch` + `currentScreenFrame` 跟踪
- 屏幕不变时跳过窗口重建
- 传递 `animateOnLaunch` 参数

### 20. NotchView spinner padding 修复 ✅
**文件:** `ClaudeIsland/UI/Views/NotchView.swift`
- ProcessingSpinner 添加 `.padding(.trailing, ...)`

### 21. NotchWindowController animateOnLaunch ✅
**文件:** `ClaudeIsland/UI/Window/NotchWindowController.swift`
- `animateOnLaunch: Bool = true` 参数
- Boot animation 只在初始启动时执行

### 22. ScrollView 包装设置菜单 ✅
**文件:** `ClaudeIsland/UI/Views/NotchMenuView.swift`
- 用 ScrollView 包装 VStack，防止菜单项超出面板高度时截断
- 适配本地化版本（保持 CustomSoundRow 替代 SoundPickerRow）

### 23. ClaudeDirPickerRow ✅
**文件:** `ClaudeIsland/UI/Components/ClaudeDirPickerRow.swift` (新增)
**文件:** `ClaudeIsland/Core/ClaudeDirSelector.swift` (新增)
**文件:** `ClaudeIsland/Core/NotchViewModel.swift`
- 原生 macOS 文件夹选择器替代文本输入
- 自动检测 / 选择文件夹两种模式
- 展开时自动调整菜单高度

---

## 改动文件清单 (31个)

| 状态 | 文件 |
|------|------|
| 修改 | ClaudeIsland/App/AppDelegate.swift |
| 修改 | ClaudeIsland/App/ScreenObserver.swift |
| 修改 | ClaudeIsland/App/WindowManager.swift |
| 修改 | ClaudeIsland/Core/Settings.swift |
| 修改 | ClaudeIsland/Models/ChatMessage.swift |
| 修改 | ClaudeIsland/Models/SessionPhase.swift |
| 修改 | ClaudeIsland/Models/SessionState.swift |
| 修改 | ClaudeIsland/Models/ToolResultData.swift |
| 修改 | ClaudeIsland/Services/Chat/ChatHistoryManager.swift |
| 修改 | ClaudeIsland/Services/Hooks/HookInstaller.swift |
| 修改 | ClaudeIsland/Services/Hooks/HookSocketServer.swift |
| 修改 | ClaudeIsland/Services/Session/AgentFileWatcher.swift |
| 修改 | ClaudeIsland/Services/Session/ClaudeSessionMonitor.swift |
| 修改 | ClaudeIsland/Services/Session/ConversationParser.swift |
| 修改 | ClaudeIsland/Services/Session/JSONLInterruptWatcher.swift |
| 修改 | ClaudeIsland/Services/State/SessionStore.swift |
| 修改 | ClaudeIsland/Services/State/ToolEventProcessor.swift |
| 修改 | ClaudeIsland/UI/Components/CustomSoundRow.swift |
| 修改 | ClaudeIsland/UI/Components/ScreenPickerRow.swift |
| 修改 | ClaudeIsland/UI/Views/ChatView.swift |
| 修改 | ClaudeIsland/UI/Views/ClaudeInstancesView.swift |
| 修改 | ClaudeIsland/UI/Views/NotchMenuView.swift |
| 修改 | ClaudeIsland/UI/Views/NotchView.swift |
| 修改 | ClaudeIsland/UI/Views/ToolResultViews.swift |
| 修改 | ClaudeIsland/UI/Window/NotchWindowController.swift |
| 修改 | ClaudeIsland/Utilities/MCPToolFormatter.swift |
| 修改 | ClaudeIsland/en.lproj/Localizable.strings |
| 修改 | ClaudeIsland/zh-Hans.lproj/Localizable.strings |
| 新增 | ClaudeIsland/Core/ClaudePaths.swift |
| 新增 | ClaudeIsland/Core/LanguageManager.swift |
| 新增 | ClaudeIsland/Core/ClaudeDirSelector.swift |
| 新增 | ClaudeIsland/UI/Components/ClaudeDirPickerRow.swift |

---

## v1.3 未采纳的改动

| 改动 | 原因 |
|------|------|
| ~~ScrollView 包装 NotchMenuView~~ | ✅ 已合并（适配本地化版本） |
| SoundPickerRow 替代 CustomSoundRow | 完全重写，冲突大 |
| ~~ClaudeDirSelector~~ | ✅ 已合并 |
| ~~ClaudeDirPickerRow~~ | ✅ 已合并 |
| 动态配置目录 UI picker | ~~已合并为 ClaudeDirPickerRow~~ |

---

## 下一步

1. Xcode 中 Clean Build Folder (Cmd+Shift+K)
2. Build (Cmd+B)
3. 测试本地化切换
4. 测试所有合并的功能
